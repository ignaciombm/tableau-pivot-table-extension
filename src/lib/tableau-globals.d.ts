// Pulls in the official Tableau Extensions API type declarations
// (https://www.npmjs.com/package/@tableau/extensions-api-types), which declare the
// global `tableau` const used throughout this app (loaded at runtime via
// tableau.extensions.1.latest.js, see index.html).
/// <reference types="@tableau/extensions-api-types" />
